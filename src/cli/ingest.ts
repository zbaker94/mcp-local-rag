// CLI ingest subcommand — stub until task-02 provides full implementation

/**
 * Run the ingest CLI subcommand.
 * @param args - Arguments after "ingest" (e.g., file/directory paths)
 */
export async function runIngest(_args: string[]): Promise<void> {
  throw new Error('ingest command is not yet implemented')
}
